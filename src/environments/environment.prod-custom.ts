import { CUSTOM_CONFIG } from '../config/custom';

export const environment = {
  production: false,
  apiEndpoint: 'http://18.216.147.38/',
  appName: CUSTOM_CONFIG.appName,
  config: CUSTOM_CONFIG
};
